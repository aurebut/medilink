<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->

## Landing pages and SEO

The user approved a real shared replacement dossier (September 2026). `ReplacementDossier` is integrated into the documents tab of both current-missions pages, with `/applications/:applicationId/dossier` APIs, versioned contract/declaration PDFs, imported supporting documents and signed copies, and explicit email recipient/document selection. The generator covers individual liberal medical replacements, with doctor/student paths; official insurance/registration/licence/authorisation documents remain imports. Imported signatures are not certified and email acceptance is not Ordre approval. Preserve the distinction between a generated draft, an imported signed copy, and sent documents. The homepage documents section now uses this actual screen with fictional data; the mobile preview stops after the first complete supporting-document row using the manifest's measured `previewHeight`, while enlargement retains the full dossier. This supersedes the old profile-documents capture and the prohibition on showing implemented contract generation/transmission; electronic signing and automatic legal validation remain unimplemented.

The user rejected the first presentation of the genuine screenshots as unattractive. The preview treatment now uses closer native component captures, a light desktop window and a compact mobile frame, with captions outside. Preserve surrounding landing copy and sections. The mobile mission preview ends after the first three complete steps (360/534 crop); its enlargement keeps all six. Keep this crop aligned with natural row boundaries when regenerating that image. Messages exclude the global navigation; all captures still use the actual product CSS without screenshot-only restyling.

The user subsequently requested aligning the real product interface to the homepage before replacing its invented screenshots. A palette-only change was rejected as visually too cheap; preserve the stronger editorial composition now implemented in the actual MessageCenter, mission folio and document register. `app/brand-tokens.css` shares locally hosted DM Sans / Instrument Serif fonts, brand blue #1952a0 and porcelain/pale-blue surfaces between public and platform layouts. `workspace-navigation.css`, `workspace-design.css`, `workspace-details.css` and `workspace-messages.css` apply these to real screens; keep success/warning/error colours distinct and retain all functional controls. Tablet message height must account for the bottom navigation after data has loaded.

The three homepage interface previews now use genuine application captures from `scripts/capture-landing-interface.mjs` with fictional API fixtures, separate desktop/mobile layouts and lossless WebP assets. Their manifest provides dimensions and content hashes for `content/landing-interface-preview.ts`; regenerate the assets after changing captured UI. Never restyle the UI only for screenshots. The public captions identify demonstration data and previews can be enlarged. The continuity copy now describes the implemented mission steps, not the previously illustrated clinical metrics. This supersedes the earlier editorial mockup requirements below while preserving surrounding landing sections and the abstract process animations.

The user then requested a third section about documents shared on the same platform, coherent with the two redesigned previews. `content/landing-documents-section.ts` adds `#documents` after `#continuite` and before testimonials; `documents-editorial.css` is scoped to `.ml-documents` and compiled with the homepage. Keep its document folio and shared-space illustration in the same ivory, sage and forest palette. This section presents documents alongside exchanges and mission tracking; do not imply automatic version synchronization, message attachments, legal validation, contract signing or universal access to private profile documents. Linked establishments can access validated supporting documents. The restoration check allows this single additional section while preserving all existing surrounding sections.

The user subsequently requested an aesthetic redesign of the homepage shared dossier (`#communication`) and mission report (`#continuite`) to match the first process section. Their previews now use `content/landing-workspace-preview.ts` and `content/landing-continuity-preview.ts`, with scoped, versioned `workspace-editorial.css` and `continuity-editorial.css` imported by the homepage. Keep their existing surrounding headings and benefits, the first process section and its animations, and all other landing pages intact. The illustrative documents, messages and metrics are static examples. The restoration check allows only these two preview regions and section style modifiers in addition to the existing explicit exceptions.

All three process steps now play at 2.5x the original speed, as explicitly requested (replacing the previous 1.5x rate). Keep that rate in the shared navigation clock, including the manual interaction delay, so every movement, transformation, reading hold and tab progression stays synchronized. Individual scene timings remain authored at 1x; do not speed them up again. Pause and reduced-motion controls remain available.

The user subsequently requested redesigning step 03 as “Concluez”: validate the payment, animate money from the establishment to the locum, then transform those same banknotes into a concise report of the whole mission. This supersedes the earlier instruction to leave step 03 intact. Keep the current steps 01 and 02 unchanged. `process-conclude-scene.ts` controls the two-state sequence, pause, visibility and reduced-motion navigation; `process-conclude-geometry.ts` shares the payment route between the markup and motion. The report uses explicitly illustrative figures, with no real payment action. Keep the left-hand copy short (payment and overall report).

The user rejected the first subtle refinement of step 03 and requested a stronger aesthetic change. The current composition uses a larger cream/sage/green fan of banknotes moving diagonally toward the locum, followed by an open editorial report without a surrounding paper card. The same three notes gather, flatten and become the report's three fine rules; a prominent 90, five day marks and two completion lines appear afterward. Keep the recipient label above its icon to clear the payment route. The existing payment confirmation aligns with the report rather than disappearing. Preserve the shared 2.5x clock and both reduced-motion states.

The user approved renaming homepage step 01 to “Matchez”, then shortened its left-hand copy to “Nous vous rapprochons selon vos critères.”, one sentence each for “Remplaçant” and “Établissement”, and “Échangez pour préciser vos conditions et préparer le contrat.” Keep this copy concise, without promising automatic legal compliance.

The user subsequently requested replacing step 02 with “Pilotez”: a continuous three-state illustration of documents exchanged between the titulaire, remplaçant and Ordre des médecins; practical information (parking, access codes, software access, coffee, contacts, hours); and an illustrative daily recap. The actual large documents must shrink and travel themselves, with no duplicate miniature courier. Five documents appear in turn. The drawn connections and moving documents share `content/process-pilot-geometry.ts`; keep their paths aligned, with a progressive reduction before departure and labels outside the connections. Practical information flows continuously as bare icons without cards from the holder/establishment to the locum, then gathers into the recap. `process-pilot-scene.ts` controls this second scene using the shared navigation clock, its own duration, visibility, pause and reduced-motion controls. Step 01's illustration and step 03 stay intact. The restoration check allows the requested copy and label changes for steps 01/02.

After approving the editorial Concluez direction, the user requested the same aesthetic improvement for Pilotez's two sequences. Its five documents now use a larger folio with a page index, a fine envelope fold and circular actors. Information travels in two coordinated waves of three bare glyphs on a pale sage surface. `pilotInformationActors` shares the information layout. Keep all three accessible states, the 2.5x shared clock, the existing first scene and the approved Concluez scene.

The user replaced Pilotez's final recap with contact between two doctors and clarified that this means coordinating during the mission, not keeping in touch afterward. The final state is `contact`: “Gardez le contact.” with the eyebrow “Pendant la mission” and an exchange about checking in after consultations. Reuse the existing holder and locum actors, remove the cabinet badge, draw a quiet connection and reveal their two short messages. The doctors remain visible throughout this transition. `pilotContactActors`, `pilotContactOrigin` and `pilotContactRoute` share the geometry; the message pulse follows the same path and stops after one exchange. Preserve the two earlier Pilotez states and reduced-motion navigation.

The user explicitly requires the landing presentation from Git `60c8e06` to be preserved. SEO and article work must not redesign the home, doctor or cabinet landing pages, rewrite their visible copy, change their navigation or calls to action, or add/remove sections unless the user explicitly requests that change. Keep guide styles isolated from the landing styles. `node scripts/check-landing-restoration.mjs` checks the original markup and assets against a running site.

The user subsequently requested navigation to the articles: keep the “Guides pratiques” links to `/guides` in the desktop and mobile menus. This is an explicit exception to the original navigation, covered by the same check.

The user also requested replacing the three homepage process previews with connected, abstract vector diagrams. Keep the illustrations in `content/landing-process-art.ts` and their scoped styles in `app/(public-home)/process-art.css`, imported by the homepage so Next.js versions the stylesheet with its content. Do not move them back into an unversioned public stylesheet: stale CSS caused black, unstyled diagrams after deployment. Preserve the process section's surrounding structure, copy, tabs, and mobile behavior. The restoration check explicitly allows these three figures and their compiled stylesheet while still checking the original page everywhere else. Verify deployed CSS and computed SVG styles after publishing changes to these illustrations.

The user then requested a continuous map-to-criteria-to-discussion transformation entirely inside tab 01, reusing the existing designs and leaving tabs 02 and 03 intact. The checklist becomes a conversation about rétrocession, payment timing and reviewing the contract together. `LandingProcess` now bundles the navigation and first-scene animation with the page; do not also load the legacy `/landing-process.js`, which remains unchanged as a reference asset. The first scene waits until its artwork is visible and supports pause and reduced motion (manually cycling through all three states); later tabs retain their original timing, keyboard and touch behavior.
