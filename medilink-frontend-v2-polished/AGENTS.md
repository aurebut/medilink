<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->

## Landing pages and SEO

The user requests that every completed modification be committed and pushed after appropriate verification, without waiting for a separate push request. Include only files belonging to the requested change.

The user subsequently supplied the complete content brief for both persona landings. `content/persona-candidate.ts` now orders the remplaçant page as matching/search and offer details (`#parcours`), discussion (`#echanges`), generated/shared documents (`#documents`), then FAQ. `content/persona-establishment.ts` orders the remplacé page as candidates (`#vivier`), transmitted reports (`#activite`), shared documents, retrocession/payment (`#paiement`), prices (`#tarifs`), then FAQ. Both retain the homepage design through scoped, versioned `app/persona-editorial.css` and `app/persona-content.css`, with demo CTAs in the hero and desktop/mobile navigation. Keep the public URLs, functional local anchors and homepage intact. This brief supersedes the earlier instruction to preserve the persona copy and section order.

Persona previews reuse the genuine messages/documents assets and add genuine desktop/mobile search, offer, candidates and shared-report captures under `public/landing-assets/persona-interface`, with fictional fixture data. `content/landing-interface-preview.ts` selects the appropriate manifest without altering the original homepage captures. `scripts/fixtures/persona-interface.mjs` and `scripts/serve-persona-fixtures.mjs` serve isolated demonstration responses; `scripts/prepare-persona-captures.mjs` converts native screenshots without rebuilding their UI. Keep accessible labels and fictional-data disclosures. The report preview shows an uploaded `OTHER` file in the shared replacement dossier; it does not demonstrate automatic clinical reporting or structured consultation metrics. Legacy daily uploads into candidate profile documents require administrative approval before cabinet access, unlike the explicitly shared dossier. Do not claim a verified network size from illustrative application counts.

The user approved marketing prices of 39 EUR per successful replacement match and 99 EUR/month for the unlimited subscription. These persona/demo offers do not modify the existing authenticated billing flow or its Stripe configuration. The automatic bank debit is explicitly labelled “À venir”; the available retrocession workflow calculates declared gross fees times the agreed percentage and records settlement, without initiating a bank debit. Preserve the distinction from the illustrative consultation-count calculation, whose equal-price assumption is visible. Validate the persona structure, demo actions, previews and FAQ with `node scripts/check-landing-restoration.mjs [base-url]`; its original homepage comparison and reference public-asset checks must remain strict.

The user requested harmonizing the remplaçant and remplacé landings with the current homepage before a later content brief. `app/persona-editorial.css` is versioned through both persona layouts and shares the blue/porcelain tokens, restrained serif headings, framed hero photograph, open sections, fine dividers, blue CTA and light FAQ/footer. Preserve the dedicated copy, forms, URLs and section order. The search and recruiting examples retain their role-specific content with the shared fictional portrait; the former dark sections now reuse genuine mission/messages captures and the homepage device stylesheet. Mobile steps and example missions form vertical lists, not the legacy horizontal carousels. Keep these styles scoped to `.persona-page`, preserve the homepage presentation, and verify the explicit preview exceptions with `node scripts/check-landing-restoration.mjs`.

The user requested a smaller desktop-only gap below the Matchez / Pilotez / Concluez navigation. In `section-spacing.css`, `.ml-process-nav` now uses a 33px bottom margin above 850px (the original 18px plus 15px). Keep the existing tablet/mobile gap, the vertically centered copy and the animation card dimensions.

The user extended the subtle section separators across the homepage, then removed the first one between the hero and process animations. Keep that first transition open, without a divider or its reserved spacing. `section-spacing.css` owns the remaining shared 1px #d8dde2 dividers, capped at 1100px, with 31px spacing (25px on mobile): after process, contributors, mission tracking, messages, documents and audiences, plus before FAQ to preserve the CTA's decorative layers. Do not restore the old `.ml-continuity-inner::after` divider in `preview-layouts.css` or introduce duplicate lines. Preserve the existing footer border and all section content.

The user removed the background cards around the doctors in the contributors section. Keep `.ml-contributor` transparent, without an outer border, rounded card background or shadow, in the versioned homepage `contributors-carousel.css`. Preserve portrait rounding, all copy, internal spacing, the desktop grid and the accessible horizontal mobile carousel.

The user subsequently removed the surrounding frame behind the Matchez / Pilotez / Concluez copy. Keep `.ml-process-panel` transparent, without a border, rounded background or shadow, in the versioned homepage `blue-editorial.css`. Preserve each animation's own `.ml-process-art` surface and rounded frame, all spacing, copy, navigation and animation behavior on desktop and mobile.

The user subsequently requested swapping both the order and visual structures of the mission tracking and conversation sections while retaining all their copy. The homepage now places `#continuite` first, with a centered heading, wide mission preview and three benefits below; `#communication` follows with grouped copy/benefits on the left and its messages preview on the right. `preview-layouts.css` provides the versioned responsive layouts. Preserve each section's texts, anchors and genuine capture, the shared device proportions, and the documents section immediately afterward. This supersedes the earlier two-column mission layout and wide conversation layout.

The user prioritized the mission preview's marketing clarity and requested document transmission to the Ordre, then rejected the excessive emphasis on documents. Keep the five milestones centered on the mission: confirmation, one combined documents step, replacement in progress, end-of-mission validation and payment/retrocession. Mention the Ordre transmission within the single documents row; do not add a separate preparation/transmission row or emphasize documents again in the overview intro and surrounding benefits. `lib/mission-progress.ts` derives document states from the authorized replacement dossier; only successful ORDER deliveries covering the current contract and letter count as sent. Sending, failed, incomplete or stale documents must not become completed milestones; transmission never implies Ordre approval. An elapsed replacement period does not imply validated completion or payment. Keep the photos, full mission date range, larger mobile helper text and the soft blue active step. The mission capture uses the later fictional `missionTrackingDossier` scenario, while the documents preview still illustrates preparation. Preserve common physical device proportions and the full unclipped mobile mission capture. This supersedes the earlier requirement to retain the six original rows. Verify with `node scripts/mission-progress.test.mjs` and regenerate only the native mission assets when adjusting this view.

The user subsequently flagged awkward mobile spacing around the mission mockup. Mobile `.ml-interface-stage` has horizontal padding only: existing grid gaps and section padding provide the vertical spacing. Do not reintroduce the old 32px top / 36px bottom stage inset, which compounded the gap before the documents section. Preserve phone dimensions, capture contents, and desktop spacing.

The user requested identical mobile mockup proportions while retaining all mission details. All three phones now share a 9:19.5 screen, a 300px maximum case width and 20px stage side padding. Only blank spacing in the real mobile mission header/timeline was tightened; its full native capture (336 x 650) keeps both photos, mission context, all six steps, statuses, dates and descriptions at unchanged font/photo sizes. Keep that full-width image inside the common fixed screen without clipping, stretching, zoom or internal scrolling. This supersedes the mission-specific automatic phone height and wider-case exceptions below. Preserve the documents/messages scrolling behavior and all desktop proportions.

The user requested a more polished presentation of the Matchez / Pilotez / Concluez copy. The versioned homepage `process-copy.css` now centers each desktop copy block vertically against its animation card, as subsequently requested after removing the surrounding frame. Keep the text left-aligned and the stacked mobile copy aligned at the top. The serif titles and introductory sentences share a common rhythm, and each existing category label sits on a separate blue line above its description, divided by fine rules. The user subsequently removed the entire Matchez closing paragraph starting “Les mêmes dates ne suffisent pas toujours.”; do not restore it or its separator/spacing. Keep the remaining wording, body descriptions at 15px, the mobile artwork-first order, and the existing navigation and animation behavior.

The user requested a smaller, more elegant homepage hero heading while preserving its complete wording, then rejected the two-size composition with a long italic continuation. `hero-frame.css` now gives the entire heading one consistent size (54px desktop maximum; 32–38px mobile). Only “avec” and “pour” remain italic and brand blue. A `.hero-title-audience` span keeps “les médecins généralistes” together on its own line; the opening wraps naturally on mobile. Keep the exact copy, Instrument Serif, centered composition and responsive wrapping, scoped to the homepage in its versioned stylesheet.

The user requested less rounded progress navigation for Matchez / Pilotez / Concluez, then found the 10px/6px/square-fill treatment too angular. The versioned homepage CSS now uses an intermediate 18px radius for the navigation and 12px for both the steps and moving fill. Keep this softer treatment on desktop and mobile, preserving the blue palette, numbered badges, timing, pause/reduced-motion behavior and keyboard/touch navigation.

The user subsequently requested identical proportions for all desktop computers. All three laptop screens now use a fixed 16:10 ratio, including a macOS toolbar with a 25:1 width/height ratio. Never derive the physical screen ratio from capture dimensions. Native desktop messages are captured using a matching real viewport height; the complete mission route uses tighter blank outer margins. Long documents scroll inside the fixed screen, including by keyboard, without zoom or image distortion. Preserve the enlarged desktop widths and all phone layouts. This supersedes earlier capture-dependent laptop heights.

The user subsequently requested moderately larger desktop mockups in the continuity and shared-documents sections. Their mirrored two-column grids use a 1200px maximum, a 64px desktop gap and 58% of the available column space for the preview; desktop preview side padding is 12px. This increases the large-screen captures by about 15% while retaining readable copy. Preserve existing tablet stacking, all phone dimensions, and the messages mockup size.

The user subsequently shortened the continuity section by removing “Quand vous en avez besoin.” from its heading and repeated closing line. Keep “où en est la mission.” highlighted with the shared `.title-accent` brand blue in its existing h2, consistent with the other landing headings.

The user subsequently requested square interface corners inside desktop mockups, especially below the macOS toolbar. The native desktop message layout and replacement dossier now have zero outer border radius above 700px; regenerate their genuine desktop captures after changes. Do not reapply rounded clipping to desktop capture images. Preserve the physical device screen/case rounding, internal controls/photos, and all mobile styling/assets. This supersedes the source-sized desktop capture corner treatment below.

The user subsequently removed the blue background frame behind all three device mockups. Keep `.ml-interface-stage` transparent, without a border, rounded background panel or tinted backdrop shadow on desktop and mobile. Preserve its spacing, the physical laptop/iPhone frames and their own subtle shadows, and the full-screen capture treatment below.

The user subsequently requested that captures fill the mockup screens. The desktop macOS window is now maximized edge to edge: preserve its slim toolbar, remove the blue wallpaper inset, secondary rounded border and floating-window shadow. On mobile, the mission phone height follows the full-width native capture plus its status/home safe areas so profiles and all six steps stay visible without side gutters, cropping or scrolling. The documents capture also fills the screen width without added picture padding. Keep authentic image proportions and no enlargement.

The user subsequently requested a macOS-style window inside each desktop laptop mockup. Above 700px, a slim decorative toolbar with three traffic lights and the MédiLink title sits above the genuine capture, inset into a blue desktop background. Keep the full image's native proportions and bottom alignment by sizing the screen around the window and capture. The window wrapper uses `display: contents` on mobile so the existing phone status, capture and home-indicator flex layout stays intact. The dots are decorative, not controls; previews still cannot enlarge. This supersedes the older prohibition on desktop window decorations.

The user then requested a middle ground: restore profile/place photos and mission context while retaining all six visible steps. The actual mobile `.mission-folio-route` now has a compact title/progress header, side-by-side establishment and replacement identities, the next-step line and tighter timeline spacing. Only the generic introductory sentence is hidden on mobile; all step descriptions, statuses and dates remain. Capture the full native mobile route with an 8px margin, not just the list. The desktop layout/capture remains unchanged. Keep the complete mobile image fitted without scrolling or zoom; this supersedes the list-only crop below.

The user subsequently requested all six mission steps visible at once in the mobile mockup. Its genuine mobile capture now crops only `.candidate-current-route-list` with a small margin, excluding the oversized profile/header area from this preview; the actual app and full desktop capture stay unchanged. Keep all six rows, statuses, helper text and dates visible proportionally inside the phone, with no internal scrolling, bottom fade or zoom. The mission phone uses slightly more available width for legibility. Regenerate this native crop rather than compressing or reconstructing the interface. This supersedes the older three-step mobile crop and scrollable mission requirements.

The user subsequently removed click-to-enlarge from all landing device previews and requested that the capture bottoms align with their frames. Previews are plain non-link containers; do not restore the lightbox, zoom cursor, image links or enlargement labels. Desktop screen proportions come from each native capture to remove blank bands. The iPhone uses proportional safe areas and a taller native mobile messages capture so the composer reaches the bottom; long mission/document captures remain scrollable within the phone, including by keyboard. Keep image proportions intact.

The user requested a more coherent use of MédiLink blue in step 01 while preserving the map's terrain colours. Its map now uses a light porcelain surround and brand-blue active pins/route; the criteria scene uses `--brand-primary` rather than muted navy, then transitions to a pale-blue discussion. Keep these colours synchronized between `process-art.css` and the inline animation in `process-first-scene.ts`. Preserve the map terrain, geometry, timing, pause/reduced-motion controls and steps 02/03.

The user subsequently requested polished computer and iPhone mockups around the three genuine landing captures. `interface-previews.css` and `landing-interface-preview.ts` now provide a graphite/silver laptop above 700px and an iPhone with separate status/Dynamic Island and home-indicator areas at narrower widths. This supersedes the earlier desktop-frame removal and unframed presentation requirements. Keep the genuine screenshots, surrounding landing copy, accessible enlargement and absence of visible captions. Desktop mission/documents captures use a native 1600px viewport to fit landscape displays without cutting content; the phone keeps true image proportions in a scrollable screen, with complete captures in the lightbox. Do not stretch a device to the full length of the document register.

The user requested carrying the approved preview design through both authenticated workspaces. `workspace-collections.css` extends the shared palette, fine rules, readable photos and document-style tabs to mission lists, applications, agendas and profile forms; `workspace-overview.css` provides the real account/establishment dashboard headers and activity lists. Use existing profile and establishment URLs with initials on missing/failed images, never the fictional preview assets in production defaults. Preserve role permissions, calendar interactions, document actions and the public landing. Dashboard and agenda photos must come from the authorized API responses, including signed establishment photo URLs where available.

The user subsequently requested larger portraits and establishment photos inside the genuine interface previews, including the shared document dossier. Keep these sizes readable in the actual messages/mission screens and display the dossier's establishment and replacement profile from existing mission data, with initials when photos are unavailable. Keep the contract's holder/replacement names separate from account identity. Regenerate all three desktop/mobile capture sets after these changes. Preserve the landing's corrected corners: source-sized radii scale using manifest dimensions, and the mobile document card sits inside the phone screen with its complete border visible.

The user requested generated human portraits and an establishment photograph inside the landing's interface previews. The fictional assets and generation prompts are in `public/landing-assets/people/`, supplied only by `scripts/fixtures/landing-interface.mjs`. The real `MessageCenter` and mission timeline display existing profile/establishment image URLs through `ProfileAvatar`, with initials on missing or failed images. Never use fictional photos as defaults for real accounts. Preserve the blue/porcelain editorial styling, surrounding landing copy, process animations and documents preview. Regenerate genuine messages/mission captures after changes. The mobile mission crop uses the manifest's measured `previewHeight` to keep the first three steps complete; its enlargement keeps all six.

The user subsequently approved removing the desktop window decoration from all three landing captures: no top toolbar, dots, title/ML mark, added border or window shadow. Keep the underlying application captures and soft stage background; the mobile phone frame remains. The visible “Voir l’interface” / “Agrandir l’aperçu” links and their reserved bottom space are removed on all screen sizes. The image itself remains an accessible link to its enlargement. This supersedes the earlier desktop window and visible enlargement-link requirements below.

The user explicitly removed the three captions beneath the genuine landing previews (their titles plus “Interface MédiLink · Données de démonstration”). Do not restore those visible captions on desktop or mobile. Each figure keeps its accessible name through `aria-label`, which also supplies the lightbox title. This supersedes earlier requirements below to show captions outside the previews; fictional fixtures and native captures remain required.

The user approved extending the shared replacement dossier with amendments, CPAM replacement attestations, replies from the Ordre, bank details, retrocession statements and payment evidence. `ReplacementDossier` now groups the register into Remplacement / Paiement / Compléments, displaying at most three rows per page on all screen sizes. Keep every document reachable, including multiple amendments/replies/other attachments, and reveal a newly uploaded file on its own category/page. Optional supporting documents are not shown as universally required. Mobile rows expose named Télécharger / Transmettre / Gérer actions with touch-sized controls; this supersedes the ellipsis described below. A released MediLink payment exposes its existing protected receipt download and billing link. Do not automatically import personal profile documents or describe the current profile as a private vault: approved profile documents can be accessible to linked recruiters. The landing uses a native capture of the selected category/page, labelled as an enlarged preview, rather than claiming to show every document simultaneously. Refresh only the documents assets/manifest after changes.

The document register subsequently gained direct per-file actions at the user's request: download available files, transmit current files, generate/add/update missing or stale files, and an accessible ellipsis for secondary management. On narrow containers the actions form a compact line beneath the name and status. Downloads save the actual file (including images); transmission opens the existing confirmation with only that file selected. Keep these visible actions when refreshing screenshots.

The user subsequently rejected the replacement dossier's stacked mobile cards and requested a beautiful, compact document list. The real `ReplacementDossier` now puts contract and supporting documents in one register with a textual status and one contextual action per row (generate, transmit, manage or add). Secondary preview/regeneration/signed-copy actions are in a document management panel. Transmission can preselect one document but always retains recipient review and explicit confirmation. Keep legal distinctions and edit/send permissions. The documents landing preview now shows the complete compact dossier; this supersedes the first-supporting-row crop rule below. Regenerate only its genuine desktop/mobile captures when adjusting this UI, preserving other previews.

The user approved a real shared replacement dossier (September 2026). `ReplacementDossier` is integrated into the documents tab of both current-missions pages, with `/applications/:applicationId/dossier` APIs, versioned contract/declaration PDFs, imported supporting documents and signed copies, and explicit email recipient/document selection. The generator covers individual liberal medical replacements, with doctor/student paths; official insurance/registration/licence/authorisation documents remain imports. Imported signatures are not certified and email acceptance is not Ordre approval. Preserve the distinction between a generated draft, an imported signed copy, and sent documents. The homepage documents section now uses this actual screen with fictional data; the mobile preview stops after the first complete supporting-document row using the manifest's measured `previewHeight`, while enlargement retains the full dossier. This supersedes the old profile-documents capture and the prohibition on showing implemented contract generation/transmission; electronic signing and automatic legal validation remain unimplemented.

The user rejected the first presentation of the genuine screenshots as unattractive. The preview treatment now uses closer native component captures, a light desktop window and a compact mobile frame, with captions outside. Preserve surrounding landing copy and sections. The mobile mission preview ends after the first three complete steps (360/534 crop); its enlargement keeps all six. Keep this crop aligned with natural row boundaries when regenerating that image. Messages exclude the global navigation; all captures still use the actual product CSS without screenshot-only restyling.

The user subsequently requested aligning the real product interface to the homepage before replacing its invented screenshots. A palette-only change was rejected as visually too cheap; preserve the stronger editorial composition now implemented in the actual MessageCenter, mission folio and document register. `app/brand-tokens.css` shares locally hosted DM Sans / Instrument Serif fonts, brand blue #1952a0 and porcelain/pale-blue surfaces between public and platform layouts. `workspace-navigation.css`, `workspace-design.css`, `workspace-details.css` and `workspace-messages.css` apply these to real screens; keep success/warning/error colours distinct and retain all functional controls. Tablet message height must account for the bottom navigation after data has loaded.

The three homepage interface previews now use genuine application captures from `scripts/capture-landing-interface.mjs` with fictional API fixtures, separate desktop/mobile layouts and lossless WebP assets. Their manifest provides dimensions and content hashes for `content/landing-interface-preview.ts`; regenerate the assets after changing captured UI. Never restyle the UI only for screenshots. The public captions identify demonstration data and previews can be enlarged. The continuity copy now describes the implemented mission steps, not the previously illustrated clinical metrics. This supersedes the earlier editorial mockup requirements below while preserving surrounding landing sections and the abstract process animations.

The user then requested a third section about documents shared on the same platform, coherent with the two redesigned previews. `content/landing-documents-section.ts` adds `#documents` after `#continuite` and before testimonials; `documents-editorial.css` is scoped to `.ml-documents` and compiled with the homepage. Keep its document folio and shared-space illustration in the same ivory, sage and forest palette. This section presents documents alongside exchanges and mission tracking; do not imply automatic version synchronization, message attachments, legal validation, contract signing or universal access to private profile documents. Linked establishments can access validated supporting documents. The restoration check allows this single additional section while preserving all existing surrounding sections.

The user subsequently requested an aesthetic redesign of the homepage shared dossier (`#communication`) and mission report (`#continuite`) to match the first process section. Their previews now use `content/landing-workspace-preview.ts` and `content/landing-continuity-preview.ts`, with scoped, versioned `workspace-editorial.css` and `continuity-editorial.css` imported by the homepage. Keep their existing surrounding headings and benefits, the first process section and its animations, and all other landing pages intact. The illustrative documents, messages and metrics are static examples. The restoration check allows only these two preview regions and section style modifiers in addition to the existing explicit exceptions.

All three process steps now play at 2.5x the original speed, as explicitly requested (replacing the previous 1.5x rate). Keep that rate in the shared navigation clock, including the manual interaction delay, so every movement, transformation, reading hold and tab progression stays synchronized. Individual scene timings remain authored at 1x; do not speed them up again. Pause and reduced-motion controls remain available.

The user subsequently requested redesigning step 03 as “Concluez”: validate the payment, animate money from the establishment to the locum, then transform those same banknotes into a concise report of the whole mission. This supersedes the earlier instruction to leave step 03 intact. Keep the current steps 01 and 02 unchanged. `process-conclude-scene.ts` controls the two-state sequence, pause, visibility and reduced-motion navigation; `process-conclude-geometry.ts` shares the payment route between the markup and motion. The report uses explicitly illustrative figures, with no real payment action. Keep the left-hand copy short (payment and overall report).

The user rejected the first subtle refinement of step 03 and requested a stronger aesthetic change. The current composition uses a larger cream/sage/green fan of banknotes moving diagonally toward the locum, followed by an open editorial report without a surrounding paper card. The same three notes gather, flatten and become the report's three fine rules; a prominent 90, five day marks and two completion lines appear afterward. Keep the recipient label above its icon to clear the payment route. The existing payment confirmation aligns with the report rather than disappearing. Preserve the shared 2.5x clock and both reduced-motion states.

The user approved renaming homepage step 01 to “Matchez”, then shortened its left-hand copy to “Nous vous rapprochons selon vos critères.”, one sentence each for “Remplaçant” and “Établissement”, and “Échangez pour préciser vos conditions et préparer le contrat.” Keep this copy concise, without promising automatic legal compliance.

The user subsequently requested replacing step 02 with “Pilotez”: a continuous three-state illustration of documents exchanged between the titulaire, remplaçant and Ordre des médecins; practical information (parking, access codes, software access, coffee, contacts, hours); and an illustrative daily recap. The actual large documents must shrink and travel themselves, with no duplicate miniature courier. Five documents appear in turn. The drawn connections and moving documents share `content/process-pilot-geometry.ts`; keep their paths aligned, with a progressive reduction before departure and labels outside the connections. Practical information flows continuously as bare icons without cards from the holder/establishment to the locum, then gathers into the recap. `process-pilot-scene.ts` controls this second scene using the shared navigation clock, its own duration, visibility, pause and reduced-motion controls. Step 01's illustration and step 03 stay intact. The restoration check allows the requested copy and label changes for steps 01/02.

After approving the editorial Concluez direction, the user requested the same aesthetic improvement for Pilotez's two sequences. Its five documents now use a larger folio with a page index, a fine envelope fold and circular actors. Information travels in two coordinated waves of three bare glyphs on a pale sage surface. `pilotInformationActors` shares the information layout. Keep all three accessible states, the 2.5x shared clock, the existing first scene and the approved Concluez scene.

The user replaced Pilotez's final recap with contact between two doctors and clarified that this means coordinating during the mission, not keeping in touch afterward. The final state is `contact`: “Gardez le contact.” with the eyebrow “Pendant la mission” and an exchange about checking in after consultations. Reuse the existing holder and locum actors, remove the cabinet badge, draw a quiet connection and reveal their two short messages. The doctors remain visible throughout this transition. `pilotContactActors`, `pilotContactOrigin` and `pilotContactRoute` share the geometry; the message pulse follows the same path and stops after one exchange. Preserve the two earlier Pilotez states and reduced-motion navigation.

The user explicitly requires the landing presentation from Git `60c8e06` to be preserved. SEO and article work must not redesign the home, doctor or cabinet landing pages, rewrite their visible copy, change their navigation or calls to action, or add/remove sections unless the user explicitly requests that change. Keep guide styles isolated from the landing styles. `node scripts/check-landing-restoration.mjs` checks the original markup and assets against a running site.

The user subsequently requested navigation to the articles: keep the “Guides pratiques” links to `/guides` in the desktop and mobile menus. This is an explicit exception to the original navigation, covered by the same check.

The user also requested replacing the three homepage process previews with connected, abstract vector diagrams. Keep the illustrations in `content/landing-process-art.ts` and their scoped styles in `app/(public-home)/process-art.css`, imported by the homepage so Next.js versions the stylesheet with its content. Do not move them back into an unversioned public stylesheet: stale CSS caused black, unstyled diagrams after deployment. Preserve the process section's surrounding structure, copy, tabs, and mobile behavior. The restoration check explicitly allows these three figures and their compiled stylesheet while still checking the original page everywhere else. Verify deployed CSS and computed SVG styles after publishing changes to these illustrations.

The user then requested a continuous map-to-criteria-to-discussion transformation entirely inside tab 01, reusing the existing designs and leaving tabs 02 and 03 intact. The checklist becomes a conversation about rétrocession, payment timing and reviewing the contract together. `LandingProcess` now bundles the navigation and first-scene animation with the page; do not also load the legacy `/landing-process.js`, which remains unchanged as a reference asset. The first scene waits until its artwork is visible and supports pause and reduced motion (manually cycling through all three states); later tabs retain their original timing, keyboard and touch behavior.
